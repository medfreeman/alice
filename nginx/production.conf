server {
  listen 8080 default_server;
  server_name aliceblogs.epfl.ch;
  passenger_enabled on;
  passenger_app_env production; 
  root /var/www/alicenew/alice/public/;
}

passenger_log_file /var/www/alicenew/alice/log/production.log;
